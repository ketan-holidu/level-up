description: "LevelUp Plugin Skill Evals - Instructor"

providers:
  - id: anthropic:messages:claude-sonnet-4-6
    config:
      max_tokens: 4096

prompts:
  - file://prompts/instructor.txt

defaultTest:
  options:
    provider:
      id: anthropic:messages:claude-sonnet-4-6

tests: file://tests/instructor.yaml
