description: "LevelUp Plugin Skill Evals - Curriculum Planner"

providers:
  - id: anthropic:messages:claude-sonnet-4-6
    config:
      max_tokens: 4096

prompts:
  - file://prompts/curriculum-planner.txt

defaultTest:
  options:
    provider:
      id: anthropic:messages:claude-sonnet-4-6

tests: file://tests/curriculum-planner.yaml
